import { AllReportsPipe } from './all-reports.pipe';

describe('AllReportsPipe', () => {
  it('create an instance', () => {
    const pipe = new AllReportsPipe();
    expect(pipe).toBeTruthy();
  });
});
