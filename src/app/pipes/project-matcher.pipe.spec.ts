import { ProjectMatcherPipe } from './project-matcher.pipe';

describe('ProjectMatcherPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectMatcherPipe();
    expect(pipe).toBeTruthy();
  });
});
