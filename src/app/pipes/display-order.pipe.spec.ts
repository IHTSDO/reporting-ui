import { DisplayOrderPipe } from './display-order.pipe';

describe('DisplayOrderPipe', () => {
    it('create an instance', () => {
        const pipe = new DisplayOrderPipe();
        expect(pipe).toBeTruthy();
    });
});
